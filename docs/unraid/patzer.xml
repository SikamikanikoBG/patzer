<?xml version="1.0"?>
<Container version="2">
  <Name>patzer</Name>
  <Repository>ghcr.io/sikamikanikobg/patzer:latest</Repository>
  <Registry>https://github.com/SikamikanikoBG/patzer/pkgs/container/patzer</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/SikamikanikoBG/patzer/discussions</Support>
  <Project>https://github.com/SikamikanikoBG/patzer</Project>
  <Changes>https://github.com/SikamikanikoBG/patzer/releases</Changes>
  <ExtraSearchTerms>chess stockfish game review lichess chess.com ollama llm coach family kids</ExtraSearchTerms>
  <Overview>
  Patzer is a self-hosted chess app: import your public Chess.com games (or play in the browser) and get a full Game Review computed by the bundled Stockfish - move classifications from Brilliant to Blunder, accuracy, estimated Elo, an eval graph, key moments and engine lines.

  The AI coach is optional and points at your own Ollama or vLLM host, so nothing is sent to a cloud service. It is render-only: every chess fact is computed by Stockfish and chess.js, and the model is only allowed to phrase what it is given, which is why a small local model works without inventing pieces that are not on the board.

  Also included: play against Stockfish at seven strength tiers, live games against other people on your server (draw offers, takebacks, rematch), multi-user profiles with a kid mode, and English, Bulgarian and Spanish in both the interface and the coach.

  Everything is in one container with a single SQLite file in /app/data - back that up and you have your whole history. No telemetry, no account, MIT licensed.
  </Overview>
  <Category>GameServers: HomeAutomation: Tools:</Category>
  <WebUI>http://[IP]:[PORT:8800]</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/SikamikanikoBG/patzer/main/docs/unraid/patzer.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/SikamikanikoBG/patzer/main/docs/unraid/patzer-icon.png</Icon>
  <Config Name="WebUI Port" Target="8800" Default="8800" Mode="tcp" Description="Port Patzer listens on." Type="Port" Display="always" Required="true" Mask="false">8800</Config>
  <Config Name="Data" Target="/app/data" Default="/mnt/user/appdata/patzer" Mode="rw" Description="Holds the SQLite database and the session secret. Back this up." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/patzer</Config>
  <Config Name="COOKIE_SECURE" Target="COOKIE_SECURE" Default="false" Mode="" Description="Set to true when you put Patzer behind a reverse proxy that terminates TLS." Type="Variable" Display="advanced" Required="false" Mask="false">false</Config>
  <Config Name="STOCKFISH_PATH" Target="STOCKFISH_PATH" Default="" Mode="" Description="Optional. Leave empty - the image ships Stockfish." Type="Variable" Display="advanced" Required="false" Mask="false"/>
  <Config Name="LICHESS_EXPLORER_URL" Target="LICHESS_EXPLORER_URL" Default="" Mode="" Description="Optional. Point the Master games panel at a self-hosted lila-openingexplorer instead of the public one." Type="Variable" Display="advanced" Required="false" Mask="false"/>
</Container>
